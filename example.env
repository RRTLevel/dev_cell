# Do not set this to True when deploying in a Production environment.
DEBUG=True

# Define a secret key and keep it secure.
SECRET_KEY=d3m0-s3cr3t-k3y

# This will display non-production banners outside of a production environment.
APPLICATION_ENVIRONMENT=local

# Set the email address that will send the emails.
EMAIL_FROM=notifications@example.com

# Admins accepts multiple name and email combinations to send error exceptions to.
ADMINS="admin:administrator@example.com" 
